__pycache__/
*.pyc
.venv/
venv/

# Raw crawl cache + the SQLite db are regenerable from scratch via `make run`;
# keeping them out of git while iterating so commits stay reviewable. Revisit
# at Stage 5 -- the completion gate wants the generated dataset committed (or
# a one-command way to regenerate it), and a re-run is exactly that command.
data/raw/
data/processed/

