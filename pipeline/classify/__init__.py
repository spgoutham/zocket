# Stage 3 (Classify) lands here: sentiment.py (VADER, off-the-shelf per the
# assessment's constraints) and category.py (a small keyword taxonomy, see
# config.yaml `classification.categories`). Both are expected to emit a
# reasoning trace alongside the label, not just the label -- see README.md
# "Session 0" for why.
#
# Not implemented yet.
