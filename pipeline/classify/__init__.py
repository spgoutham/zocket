# Stage 3 (Classify): sentiment.py (VADER, off-the-shelf per the assessment's
# constraints) and category.py (a small keyword taxonomy, see config.yaml
# `classification.categories`).
#
# Not implemented yet.
