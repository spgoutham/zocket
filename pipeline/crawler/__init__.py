# Stage 1 (Crawl / Extract) lands here: an HN Algolia client with pagination,
# throttling + jitter, exponential backoff on transient failures, and raw
# payload persistence to disk before any transformation happens.
#
# Not implemented yet -- see README.md "Session 0" for the design decisions
# this stage will follow (source choice, politeness parameters, caching).
