# Stage 4 (Evaluate) lands here: compares classifier output against a
# hand-labeled sample (eval/labeled_sample.csv, ~20-30 items) and reports
# accuracy + a confusion matrix + where it fails.
#
# Not implemented yet.
